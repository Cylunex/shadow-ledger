---
name: ledger-research
description: 查询个人收支、消费频率和预算，用 Ledger 确定性数字与查询凭证回答；不创建或确认记录。
---

以当前 research 工具目录为准。先 overview，需明细才 search；商家/商品统计用 entity_insights，
建议用 attention。金额保留原币种，不从分页记录自行合计，不把整单金额当商品单价。

每个主要数字保留 query_fingerprint、币种和 status。partial 说“已知值”并指出未知/截断；
projected 说“预计”，不描述为事实。explain 可验证结构化数字声明，但不能证明任意自然语言正确。
引用只给 shadow URI，不索取原文或导出。遇权限失败停止；目录变化重新获取，不能猜测旧工具。
